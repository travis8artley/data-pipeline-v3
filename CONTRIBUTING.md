# Contributing

## Development Loop
1. Keep changes scoped to one stage where possible.
2. Add tests for each new transform rule.
3. Run `python -m pytest -q` before push.

## Commit Style
Use conventional prefixes (`feat`, `fix`, `docs`, `ci`).
